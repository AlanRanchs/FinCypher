// Leave empty by default so production (GitHub Pages) uses frontend-only local mode.
// For local backend testing, set VITE_API_BASE=http://127.0.0.1:8000
export const API_BASE = import.meta.env.VITE_API_BASE || '';
